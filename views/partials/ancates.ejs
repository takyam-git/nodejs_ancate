<% if( docs ){ %>
	<table>
		<thead>
			<tr>
				<th>アンケート名</th>
				<th>公開予定日</th>
				<th>アンケート作成日</th>
			</tr>
		</thead>
		<tbody>
			<% for(var doc in docs){ %>
				<tr>
					<td><%= docs[doc].name %></td>
					<td><%= docs[doc].unpublish %></td>
					<td><%= docs[doc].created_on %></td>
					<td><a href="#" class="edit_ancate" id="<%= docs[doc]._id %>">編集</a></td>
				</tr>
			<% } %>
		</tbody>
	</table>
<% } %>